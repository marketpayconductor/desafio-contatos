package com.conductor.desafiocontatos;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class. Used to bootstrap our application.
 */
@SpringBootApplication
public class DesafioContatosApplication {

    public static void main(String[] args) {
        SpringApplication.run(DesafioContatosApplication.class, args);
    }
}
